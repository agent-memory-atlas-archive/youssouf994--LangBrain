# Dipendenze dirette di LangBrain, con versione esatta (verificate con la suite di test).
# L'elenco completo, transitive incluse, è in requirements.lock. Python 3.12 o superiore.
# Per aggiornare: modificare le versioni qui, ricreare un ambiente pulito, rigenerare requirements.lock e rilanciare i test.
langgraph==1.2.11
langgraph-checkpoint-sqlite==3.1.1
langchain-core==1.6.1
fastapi==0.141.1
uvicorn[standard]==0.52.4
pydantic==2.13.5
aiosqlite==0.22.1
python-dotenv==1.2.3
httpx==0.28.1
openai==3.7.0
